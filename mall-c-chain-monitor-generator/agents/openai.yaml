interface:
  display_name: "Business SLI Monitor"
  short_description: "Generate one Guance monitor JSON for business-chain and overall-system SLI monitors."
  default_prompt: "Use $mall-c-chain-monitor-generator to generate one Guance monitor JSON from a customer key business interface table, including per-chain and overall business-system SLI monitors, then export and validate DQL."
